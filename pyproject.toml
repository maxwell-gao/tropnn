[project]
name = "tropnn"
version = "0.1.0"
description = "Minimal pairwise comparison lookup-table neural layer library"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [
    { name = "Senquan(Maxwell) Gao" }
]
keywords = ["pairwise", "lookup-table", "neural-network", "torch", "teaching"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
]
dependencies = [
    "numpy>=2.2.6",
    "tilelang>=0.1.9",
    "torch>=2.9.1",
]

[project.optional-dependencies]
cpu = [
    "ziglang==0.16.0",
]
dev = [
    "pytest>=9.0.2",
    "ruff>=0.1.0",
]

[project.scripts]
tropnn-emnist-payload-width = "tropnn.tools.emnist_payload_width:main"
tropnn-emnist = "tropnn.examples.emnist:main"
tropnn-recovery-lut-structures = "tropnn.examples.recovery_lut_structures:main"
tropnn-scaling-benchmark = "tropnn.tools.scaling_benchmark:main"
tropnn-dense-projection-fit = "tropnn.tools.dense_projection_fit:main"
tropnn-anchor-policy-benchmark = "tropnn.tools.anchor_policy_benchmark:main"
tropnn-paged-anchor-benchmark = "tropnn.tools.paged_anchor_benchmark:main"
tropnn-soa-anchor-benchmark = "tropnn.tools.soa_anchor_benchmark:main"
tropnn-tree-kernel-benchmark = "tropnn.tools.tree_kernel_benchmark:main"
tropnn-emnist-payload-dtype-sweep = "tropnn.tools.emnist_payload_dtype_sweep:main"
tropnn-emnist-discrete-payload = "tropnn.tools.emnist_discrete_payload:main"
tropnn-emnist-route-conditioned = "tropnn.tools.emnist_route_conditioned:main"
tropnn-partition-geometry-probe = "tropnn.tools.partition_geometry_probe:main"
tropnn-emnist-braid-generators = "tropnn.tools.emnist_braid_generators:main"
tropnn-emnist-cross-layer-anchor-sharing = "tropnn.tools.emnist_cross_layer_anchor_sharing:main"

[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
include-package-data = false

[tool.setuptools.package-data]
tropnn = ["kernels/src/*.zig"]

[tool.setuptools.package-dir]
tropnn = "."

[tool.ruff]
line-length = 150
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I", "W"]
